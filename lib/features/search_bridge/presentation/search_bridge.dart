import 'package:flutter/material.dart';

class SearchBridgeScreen extends StatelessWidget {
  const SearchBridgeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: const Text("Search for a Bridge Here!"),
      ),
    );
  }
}
